"""Distillate integrations — add-on backends that plug into the core pipeline.

Integrations live outside the core read/annotate loop and are loaded on demand.
Currently shipped integrations:
    - remarkable: read papers on a reMarkable tablet, highlights extracted from .rmscene bundles.
"""
